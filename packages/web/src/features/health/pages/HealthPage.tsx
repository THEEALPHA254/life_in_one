import { PagePlaceholder } from "../../_shared/PagePlaceholder";
export function HealthPage() {
  return (
    <PagePlaceholder
      title="Health"
      subtitle="v1: manual entry (weight, steps, custom metrics). Provider sync (Google Health) lands later."
    />
  );
}
