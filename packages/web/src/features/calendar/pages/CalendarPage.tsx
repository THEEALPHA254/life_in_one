import { PagePlaceholder } from "../../_shared/PagePlaceholder";
export function CalendarPage() {
  return <PagePlaceholder title="Calendar" subtitle="Month / week / day + two-way Google Calendar sync." />;
}
